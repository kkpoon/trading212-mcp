[project]
name = "trading212-mcp"
version = "0.1.0"
description = "MCP Server to use Trading212 API"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.27.0",
    "pydantic-settings>=2.14.0",
    "python-dotenv>=1.2.2",
]
